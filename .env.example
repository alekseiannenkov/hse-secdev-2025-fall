# Пример локальной конфигурации приложения (не коммитить как .env)

APP_ENV=dev
LOG_LEVEL=INFO

SECRET_KEY=change-me
DB_URL=sqlite:///./wishlist.db

# Дополнительные настройки (совпадают с дефолтами из app/core/config.py)

ACCESS_TOKEN_EXPIRE_MINUTES=1440

HTTP_CONNECT_TIMEOUT=2.0
HTTP_READ_TIMEOUT=4.0
HTTP_TOTAL_TIMEOUT=6.0
HTTP_MAX_RETRIES=3
