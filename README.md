# Wishlist

Приложение для ведения личного списка желаемых вещей (wishlist) с возможностью сортировки, фильтрации и экспорта. Каждый пользователь видит только свои записи.

## Содержание
- [Требования окружения](#требования-окружения)
- [Установка и запуск](#установка-и-запуск)
- [Конфигурация](#конфигурация)
- [Тесты и качество](#тесты-и-качество)
- [API](#api)
- [Архитектура](#архитектура)
- [Безопасность](#безопасность)
- [Отладка CI](#отладка-ci)
- [Лицензия](#лицензия)

## Требования окружения
- Python >= 3.10
- Git
- make (опционально)
- PostgreSQL или SQLite (по умолчанию используется SQLite)

## Установка и запуск
```bash
git clone https://github.com/yourusername/wishlist.git
cd wishlist

python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

Инициализация базы данных:
```bash
alembic upgrade head
```

Запуск приложения:
```bash
uvicorn app.main:app --reload
```

После запуска сервер будет доступен по адресу:
👉 http://127.0.0.1:8000

Документация API:
- Swagger UI → http://127.0.0.1:8000/docs
- ReDoc → http://127.0.0.1:8000/redoc

## Конфигурация
- Переменные среды:
  - `APP_ENV` — окружение (`dev`, `prod`)
  - `DATABASE_URL` — строка подключения (например, `sqlite:///./wishlist.db`)
  - `SECRET_KEY` — ключ для JWT
- Все секреты и токены хранятся вне репозитория (например, в `.env`).

## Тесты и качество
Запуск тестов:
```bash
pytest -q
```

Линтеры и форматирование:
```bash
ruff check --fix .
black .
isort .
```

Полная проверка перед коммитом:
```bash
pre-commit run --all-files
```

## API
Базовый URL: `/api/v1`

### Основные эндпоинты:
| Метод | URL | Описание |
|--------|-----|-----------|
| `POST` | `/auth/register` | Регистрация пользователя |
| `POST` | `/auth/login` | Авторизация |
| `GET` | `/wishes` | Получить список желаний |
| `POST` | `/wishes` | Добавить новое желание |
| `GET` | `/wishes/{id}` | Получить желание по ID |
| `PUT` | `/wishes/{id}` | Обновить желание |
| `DELETE` | `/wishes/{id}` | Удалить желание |
| `GET` | `/wishes?price<` | Фильтрация по цене |

Пример ошибки:
```json
{
  "code": "VALIDATION_ERROR",
  "message": "price_estimate must be non-negative",
  "details": {"field": "price_estimate"}
}
```

## Архитектура
- **FastAPI** для REST API
- **SQLAlchemy** ORM
- **Pydantic** для валидации схем
- **JWT** для аутентификации
- **Alembic** для миграций
- **Pytest** для тестов

Основные модули:
- `app/main.py` — точка входа
- `app/models.py` — ORM-модели (`User`, `Wish`)
- `app/schemas.py` — Pydantic-схемы
- `app/api` — роуты (`auth`, `wishes`)
- `app/core/security.py` — JWT и аутентификация

## Безопасность
- Все эндпоинты защищены авторизацией (только владелец видит свои данные)
- Проверка корректности URL и числовых полей
- Исключение IDOR (нет прямого доступа к чужим ресурсам)
- Секреты вне кода и репозитория

## Отладка CI
- Проверить вкладку **Actions → последний Failed шаг**
- Типичные фиксы:
  ```bash
  ruff --fix .
  black .
  isort .
  pytest -q
  ```

## Лицензия
MIT
